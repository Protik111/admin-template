import {
    useQuery,
    useMutation,
    useQueryClient,
    useInfiniteQuery,
  } from "react-query";
import { QUERY_KEYS } from "./queryKeys";

//auth queries
export const userSignInAccount = () => {
    const 
    return useMutation(
        () => {
            return () => console.log("logged");
          }
    )
}